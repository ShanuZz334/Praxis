import { generateRegistrationOptions } from '@simplewebauthn/server'; import mongoose from 'mongoose'; async function test() { try { const _id = new mongoose.Types.ObjectId(); const options = await generateRegistrationOptions({ rpName: 'Test', rpID: 'localhost', userID: new Uint8Array(Buffer.from(_id.toString())), userName: 'test@example.com', userDisplayName: 'Test', attestationType: 'none', excludeCredentials: [], authenticatorSelection: { authenticatorAttachment: 'platform', residentKey: 'required', userVerification: 'preferred', }, }); console.log('SUCCESS'); } catch(err) { console.error('FAILED:', err.message); } } test();
